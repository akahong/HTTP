<html></html>称为根标签，所有的网页标签都在<html></html>中
注释代码：<!--注释文字 -->

<head> 标签用于定义文档的头部，它是所有头部元素的容器。头部元素有<title>、<script>、 <style>、<link>、 <meta>等标签
<title>标签：在<title>和</title>标签之间的文字内容是网页的标题信息，它会出现在浏览器的标题栏中。
<style>标签：定义css样式
<link>标签：链接外部文档
<meta>标签：html文档的设置

<p>标签：<p>段落文本</p>
  <em>需要强调的文本</em> 
  <strong>需要强调的文本</strong>
  两者在强调的语气上有区别:<em> 表示强调，<strong> 表示更强烈的强调。并且在浏览器中<em> 默认用斜体表示，<strong> 用粗体表示。
  <span>标签是没有语义的，它的作用就是为了设置单独的样式用的。
  
<hx>标签：<hx>标题文本</hx> (x为1-6)。<h1>是最高的等级

<q>标签：<q>引用文本</q> 短文本引用  引用的文本不用加双引号，浏览器会对q标签自动添加双引号

<blockquote>标签：<blockquote>引用文本</blockquote> 长文本引用

<address>标签：<address>联系地址信息</address> 为网页加入地址

<code>标签：<code>代码语言</code> 显示单行代码
     如果是多行代码，可以使用<pre>标签。
     
<pre>标签：<pre>语言代码段</pre>
    <pre> 标签的主要作用:预格式化的文本。被包围在 pre 元素中的文本通常会保留空格和换行符。
    
<ul>标签：添加无序信息列表
    ul-li是没有前后顺序的信息列表
    
<ol>标签：添加有序列表
    ol-li是有前后顺序的信息列表
    
<div>标签：<div>标签的作用就相当于一个容器 页面的逻辑部分（它是页面上相互关联的一组元素。如网页中的独立的栏目版块）
    id属性：id属性来为<div>提供唯一的名称
    
<table>标签：表格
    <table>…</table>：整个表格以<table>标记开始、</table>标记结束。
    <tbody>…</tbody>：如果不加<thead><tbody><tfooter> , table表格加载完后才显示。
        加上这些表格结构， tbody包含行的内容下载完优先显示，不必等待表格结束后在显示，
        同时如果表格很长，用tbody分段，可以一部分一部分地显示。（通俗理解table 可以按结构一块块的显示，不在等整个表格加载完后显示。
    <tr>…</tr>：表格的一行，所以有几对tr 表格就有几行
    <td>…</td>：表格的一个单元格，一行中包含几对<td>...</td>，说明一行中就有几列。
    <th>…</th>：表格的头部的一个单元格，表格表头
    <caption>标签：为表格添加标题
    summary属性：为表格添加摘要  在html页面不显示
    table表格在没有添加css样式之前，在浏览器中显示是没有表格线的
    表头，也就是th标签中的文本默认为粗体并且居中显示
    
<a>标签：链接到另一个页面
    href属性：目标网址
        mailto：链接Email地址
        如：<a href="mailto:yy@imooc.com?cc="抄送"&bcc="密送"&subject="邮件主题"&body="邮件内容"">链接显示文本</a>
    title属性：鼠标滑过显示的文本
    target属性：_blank：在新的浏览器窗口中打开链接
    
<img>标签：为页面插入图片
    src属性：标识图像的位置
    alt属性：指定图像的描述性文本，当图像不可见时（下载不成功时），可看到该属性指定的文本
    title属性：提供在图像可见时对图像的描述(鼠标滑过图片时显示的文本)
    如：<img src="图片地址" alt="下载失败时的替换文本" title = "提示文本">
    
<form>标签：表单
    <form> ：<form>标签是成对出现的，以<form>开始，以</form>结束
    action属性 ：浏览者输入的数据被传送到的地方
    method属性 ： 数据传送的方式（get/post）
    所有表单控件（文本框、文本域、按钮、单选框、复选框等）都必须放在 <form></form> 标签之间（否则用户输入的信息可提交不到服务器上！）
    <input>标签：没有结束标签
        type属性：
            当type="text"时，控件为文本输入框;
            当type="password"时, 控件为密码输入框
            当 type="radio" 时，控件为单选框
            当 type="checkbox" 时，控件为复选框
            当type="submit"时，控件为提交按钮
            当type="reset"时：控件为重置按钮
        name属性：为文本框命名，以备后台程序ASP 、PHP使用
        checked属性：当设置 checked="checked" 时，该选项被默认选中（用于单选框和复选框）
        placeholer／value属性：为文本输入框设置默认值。(一般起到提示作用)（用于输入框）
        value属性：提交数据到服务器的值（后台程序程序使用）（用于单选框和复选框）
        value属性：按钮上显示的文字（用于提交按钮）
        注：同一组的单选按钮，name 取值一定要一致
    <textarea>标签：文本域，支持多行文本输入
        <textarea>标签是成对出现的，以<textarea>开始，以</textarea>结束。
        cols属性 ：多行输入域的列数
        rows属性 ：多行输入域的行数
        在<textarea></textarea>标签之间可以输入默认值
    <select>标签：下拉列表框
        multiple属性：multiple="multiple"属性，就可以实现多选功能
        <option>标签：选项
            value属性：向服务器提交的值
            selected属性：selected="selected"属性，则该选项就被默认选中
    <lable>标签：label标签不会向用户呈现任何特殊效果，它的作用是为鼠标用户改进了可用性。
                如果你在 label 标签内点击文本，就会触发此控件。就是说，当用户单击选中该label标签时，
                浏览器就会自动将焦点转到和标签相关的表单控件上
            for属性：跳转至指定控件的id
            注：标签的 for 属性中的值应当与相关控件的 id 属性值一定要相同
        
&nbsp;空格

空标签：
<br />标签：换行
<hr />标签：水平线
<img />标签：图片
